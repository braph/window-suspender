- Error handling (e.g. invalid regexes)
- Hints for syntax errors
- Warning message for colliding suspend-commands
- Only subscribe events we're really interested in.
  Determine this while parsing the configuration. (maybe)
- Destroy the app
- Add DBus?
- Keywords for logic: 'and', 'or', 'not', maybe 'in'
- XOR ?
- Geometry condition, Geometry actions
- Better string parsing
